@using MyVocabulary.Models;
@model IEnumerable<LearnWord>
@{
    ViewBag.Title = "Index";
}

<h2>Learned Words</h2>
@if(Model.Count() == 0)
{
    <p>No learned words</p>
}
else
{
    foreach(var word in Model)
    {
        <p>@word.WordString</p>
    }
}

